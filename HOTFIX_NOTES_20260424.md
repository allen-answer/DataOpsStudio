# 修复说明

本修复包基于上一版 Windows 离线部署包，补充以下问题修复：

## 1. 双 SQL + 字段映射 + 主键兼容

修复前：

- 目标 SQL 已经把字段 `alias` 成与源字段同名时
- 如果仍填写了字段映射，例如 `CUST_NO -> fund_account`
- 可能报错：

```text
target row 1 missing key columns: fund_account
```

修复后：

- 主键字段仍需填写
- 目标侧 key 会优先匹配目标结果里的实际字段名
- 若映射字段不存在，会自动回退到原字段名
- 主键字段值匹配时会自动去除字符串前后空格，例如 `"30431260  "` 和 `"30431260"` 会视为同一个 key

## 2. 忽略字段与主键匹配说明

修复前：

- 当源端主键值带尾部空格、目标端不带空格时，两边数据会进入 `only_source` / `only_target`
- 此时还没有进入字段级比较，因此忽略字段不会生效

修复后：

- 主键匹配会自动去除字符串前后空格
- 主键匹配成功后，忽略字段才会按规则参与 `diff/same` 判断

## 3. 数据源测试反馈优化

修复前：

- 测试连接返回原始 JSON / 原始错误文本，可读性一般

修复后：

- 成功显示：`连接成功`
- 失败显示：`连接失败：错误信息`

## 4. 编辑区展开/收起优化

修复前：

- 编辑区展开后，收起入口不够直观

修复后：

- 打开后按钮显示 `收起`
- 数据源编辑区增加明确的 `收起` 按钮

## 5. 字段映射输入体验优化

修复后支持：

- 多行一条规则
- `->`
- `=>`
- `→`
- `:`
- `：`

示例：

```text
CUST_NO -> fund_account
SRC_AMT -> BALANCE
```

## 6. DB2 数据源连通性测试修复

修复前：

- DB2 数据源测试使用 `select 1 as ok`
- 部分 DB2 环境会报：

```text
SQL0104N An unexpected token "END-OF-STATEMENT" was found following "select 1 as ok"
```

修复后：

- DB2 数据源测试改为：

```sql
select 1 as ok from sysibm.sysdummy1
```

## 7. 血缘来源表别名展开增强

修复前：

- 某些子查询别名字段，例如 `b.BRANCH_CODE`
- 页面“来源表”可能只显示 `b`

修复后：

- 如果 `b` 是子查询别名，会尽量展开到子查询内部真实来源表
- 即使子查询内部使用 `select *`，也会显示子查询中识别到的真实表

## 8. 血缘分析结果导出

新增：

- 血缘分析页面支持导出 Excel
- 血缘分析页面支持导出 JSON

Excel 包含以下 sheet：

- 来源表
- 字段血缘
- 落表字段映射
- JOIN
- WHERE
- GROUP_BY
- UNION

## 9. 结果历史按任务筛选

新增：

- 执行历史页面支持按任务筛选
- 支持查看指定任务的历史运行记录
- 保留重新下载 Excel / JSON 和删除历史结果能力

## 10. 任务复制能力

新增：

- 任务列表支持一键复制任务
- 复制后的任务会生成独立任务，默认名称为 `原任务名 副本`
- 复制后自动展开编辑区，并聚焦任务名称
- 可直接重命名，也可以修改 SQL、字段映射、忽略字段、数值容忍、主键字段和数据源配置

## 11. 字段名大小写兼容

修复前：

- 部分数据库会把未加引号的字段名返回为大写，例如源 SQL 返回 `SEC_CODE`
- 如果任务主键填写为 `sec_code`，会报：

```text
source row 1 missing key columns: sec_code
```

修复后：

- 主键字段匹配忽略字段名大小写
- 字段映射源字段和目标字段匹配忽略字段名大小写
- 忽略字段匹配忽略字段名大小写
- 结果明细仍保留数据库实际返回的原字段名

注意：

- 如果目标侧字段名也不一致，例如 `SEC_TYPE` 对应 `stock_type`
- 需要在字段映射中填写 `SEC_TYPE -> stock_type`
- 这样忽略 `SEC_TYPE` 时，目标侧对应字段也会一起按映射关系处理

## 12. 编辑区预览使用当前草稿 SQL

修复前：

- 在任务编辑区修改 SQL 后，如果没有先点击 `保存`
- 点击 `预览源` / `预览目标` 仍会使用已保存任务中的旧 SQL
- 可能出现数据库客户端执行当前 SQL 有数据，但页面预览返回 `rows: []`

修复后：

- 编辑区展开时，预览会直接使用当前表单里的 SQL 和数据源
- 编辑区收起时，预览仍使用已保存任务配置

## 13. 字段映射为空时按查询字段顺序自动映射

新增：

- 当字段映射为空时，如果源 SQL 和目标 SQL 返回列数一致，会按查询结果字段顺序自动建立映射
- 示例：

```text
源字段：CUST_NO, SEC_CODE, TOTAL_ASSET
目标字段：client_id, stock_code, last_value
自动映射：
CUST_NO -> client_id
SEC_CODE -> stock_code
TOTAL_ASSET -> last_value
```

适用场景：

- 双 SQL 模式下两边字段名不同，但 SELECT 字段顺序一一对应
- 主键字段仍按源字段填写，例如 `CUST_NO, sec_code`

规则：

- 只有字段映射完全为空时才启用自动顺序映射
- 如果已经填写字段映射，以手工配置为准
- 如果源/目标返回列数不一致，不自动猜测映射

## 14. 历史结果多选合并导出

新增：

- 执行历史页面支持勾选多个历史运行结果
- 支持选择要合并导出的 sheet：
  - `汇总对照`
  - `only_source`
  - `only_target`
  - `diff`
  - `same`
- 可以只选择多个历史结果中的 `汇总对照` sheet，合并生成一个新的 Excel
- 每个历史运行的 sheet 会写入新 Excel 的独立 sheet 页
- 生成的 sheet 名会自动带上运行 ID 后缀，避免重名

说明：

- 只有存在原始 Excel 文件的历史结果可以参与合并导出
- 如果历史结果已删除 Excel，只保留 JSON，则该行会显示 `无 Excel`

## 15. 首页工作台界面重构

新增/调整：

- 首页改为任务工作台布局
- 左侧为任务列表，支持按任务名、主键、SQL 搜索
- 左侧支持按 `单 SQL` / `双 SQL` 筛选
- 右侧只展示当前选中的任务详情，避免任务多时页面无限变长
- 任务详情中保留：
  - 执行
  - 后台执行
  - 预览源
  - 预览目标
  - 复制
  - 删除
  - 保存修改
- 新增任务作为左侧任务列表中的独立入口
- 数据源管理移入右侧抽屉：
  - 新增数据源
  - 编辑数据源
  - 删除数据源
  - 测试连接
- 顶部保留：
  - 数据源
  - 执行历史
  - SQL 血缘
  - 驱动检测
  - 新增任务

保留：

- 原有后端接口不变
- 原有 JSON 配置结构不变
- 原有任务执行、预览、复制、数据源测试能力不变

## 16. 任务详情保存按钮遮挡预览结果修复

修复前：

- 任务详情底部保存按钮使用固定/吸底效果
- 当点击 `预览源` / `预览目标` 后，预览输出可能被保存按钮遮挡

修复后：

- 保存按钮改为普通底部操作区
- 预览结果按页面流正常展示，不再被按钮覆盖

## 17. MySQL Docker 容器连接验证说明

本次使用以下 MySQL 容器信息做验证：

```text
容器名：mysql-db
镜像：mysql:latest
端口：3306
数据库：appdb
```

验证结论：

- 应用容器内缺少 MySQL 驱动时，会提示 `MySQL driver is not installed`
- 临时安装 `pymysql` 后，MySQL 8 默认认证还需要 `cryptography`
- 应用容器与 `mysql-db` 不在同一个 Docker 网络时，应用容器内不能直接用 `mysql-db` 作为 Host
- 本地 Docker Desktop 场景下，应用容器使用 `host.docker.internal:3306` 测试连接成功
- 使用 MySQL 容器 IP `172.17.0.3:3306` 也可连接成功，但容器 IP 不稳定，不建议写入长期配置

建议：

- 离线 Windows 使用 MySQL / OceanBase MySQL 时，准备 `pymysql` wheel
- 若连接 MySQL 8 默认认证用户，同时准备 `cryptography` wheel

## 18. 首页最近执行展示优化

调整前：

- 首页底部有独立的 `最近执行` 模块
- 与执行历史页功能重复
- 在任务工作台布局下占用页面空间，干扰任务编辑和预览

调整后：

- 移除首页底部独立 `最近执行` 模块
- 每个任务详情标题下展示该任务最近一次执行摘要
- 摘要包含：
  - 执行时间
  - 耗时
  - `diff`
  - `same`
  - Excel / JSON 下载入口
- 没有执行记录的任务显示 `暂无执行记录`

保留：

- 完整历史列表、按任务筛选、多选合并导出仍在 `执行历史` 页面中完成

## 19. SQL 辅助打磨

调整前：

- 血缘分析页面承载了较多 SQL 辅助功能，实际使用时不够聚焦
- 任务编辑区里的候选 key 提示规则偏窄
- `CLIENT_ID`、`FUND_ACC_NO`、`SEC_CODE` 等常见字段可能没有被提示为候选 key
- 使用 `SELECT *` 时，用户容易误以为提取字段功能失效

调整后：

- 血缘分析页面只保留当前 SQL 文本框格式化能力
- 任务编辑区源 SQL / 目标 SQL 增加轻量辅助能力：
  - 格式化
  - 提取字段
  - 候选 key 提示
  - 只读校验
- 候选 key 规则扩展到：
  - `id`
  - `uuid`
  - `*_id`
  - `*_no`
  - `*_num`
  - `*_code`
  - `*_cd`
  - 包含 `num` 的字段
- 字段提取改为直接从 `SELECT` 表达式读取输出字段，结果更稳定
- 前端增加候选 key 兜底判断
- `SELECT *` 场景明确提示：静态解析无法展开真实表字段，需要显式列出字段名

说明：

- 候选 key 只是静态启发式提示，不会自动替代人工主键配置
- SQL 辅助不连接数据库、不读取表结构、不执行 SQL

## 20. 血缘分析增强与页面布局优化

调整前：

- 血缘分析主要面向单条 `SELECT` / `INSERT SELECT`
- 遇到 ETL 脚本中的 `${v_qmrq_m}` 等变量时，变量信息不会单独展示
- 脚本中混有 `DELETE`、`COMMIT` 等语句时，整体解析容易失败或需要人工拆 SQL
- 复杂 CTE、派生表、嵌套子查询、`UNION` 子查询的字段来源展开不够稳定
- 血缘页面的分析、格式化和导出按钮布局分散，宽屏和窄屏下都不够紧凑
- 任务预览结果区域偏小，查看 JSON 预览不方便

调整后：

- 血缘分析支持识别脚本变量和占位符：
  - `${v_qmrq_m}`
  - `:biz_date`
  - `@biz_date`
- 支持脚本级解析，可跳过 `DELETE`、`COMMIT` 等非血缘主体语句，提取多段 `INSERT INTO ... SELECT ...`
- 支持从 `CREATE PROCEDURE` / `CREATE FUNCTION` 过程体中提取可分析的 `INSERT SELECT`
- 增强 CTE、复杂派生表、嵌套子查询和 `UNION` 子查询字段到真实来源表的展开能力
- 来源表结果中过滤目标落表，避免目标表误入来源表
- 字段血缘和落表字段映射新增变量引用列
- 血缘分析结果新增“脚本变量”展示区
- 血缘 Excel 导出新增 `脚本变量` sheet
- 血缘页面操作区改为集中按钮栏：
  - `分析血缘`
  - `格式化`
  - `导出 Excel`
  - `导出 JSON`
- 任务预览输出区域放大，并支持在页面中手动调整高度

验证：

- 使用 `dwd_acc_cptl_acc_A.txt` 这类 ETL 脚本验证，可识别多段落表映射、`${v_qmrq_m}` 变量和复杂子查询来源表
- Docker 容器内完整单元测试通过：

```bash
docker compose exec app python -m unittest discover -s tests -v
```

## 21. 多脚本 ETL 流程分析原型

新增：

- 新增入口：

```text
/lineage/batch
```

- 支持一次上传多个 `.sql` / `.txt` 文件
- 支持上传包含 `.sql` / `.txt` 的 `.zip` 脚本包
- 输出“流程总览”，包含：
  - 脚本数
  - 成功/失败文件数
  - 读取表数
  - 写入表数
  - 表级数据流数
  - 跨脚本依赖数
  - 风险提示数
- 输出“脚本清单”，展示每个脚本的读取表、写入表、变量、语句数和解析状态
- 输出“表级数据流”，展示 `来源表 -> 目标表`，并标出脚本和语句序号
- 输出“跨脚本依赖”，当一个脚本写出的表被另一个脚本读取时生成依赖边
- 输出“风险提示”，包含：
  - 解析失败
  - `SELECT *`
  - 疑似动态 SQL
  - 多脚本写同一目标表
  - 外部源表
  - 最终产物

入口调整：

- 首页顶部增加“多脚本分析”
- 单脚本血缘页面顶部增加“多脚本分析”

验证：

- 使用 `dwd_acc_cptl_acc_A.txt` 通过 `/lineage/batch` 上传验证，页面可返回流程总览、脚本清单、表级数据流和风险提示
- Docker 容器内完整单元测试通过：

```bash
docker compose exec app python -m unittest discover -s tests -v
```

## 22. 运维与血缘增强

新增：

- 后台任务取消：
  - 新增 `POST /api/runs/{job_id}/cancel`
  - 首页后台执行后展示 `取消后台任务`
  - 取消为协作式取消，当前数据库查询阶段结束后停止后续流程
- 查询分块读取：
  - 任务配置新增 `fetch_chunk_size`
  - 查询时按批次 `fetchmany`
  - 后台任务状态显示已读取行数
- SQL 血缘图形化展示：
  - 单脚本血缘页面展示表级 `来源表 -> 目标表`
  - 多脚本 ETL 页面展示表级流程图
- Schema 元数据导入：
  - 单脚本血缘页面支持上传 Schema JSON / SQL / TXT
  - 用于辅助展开 `SELECT *` 字段级血缘
  - 支持 `{ "schema.table": ["col1", "col2"] }`
  - 支持 `{ "tables": [{"table": "schema.table", "columns": ["col1", "col2"]}] }`
  - 支持从 `CREATE TABLE` DDL 中提取字段
  - 支持简单文本格式 `schema.table,column`
- 配置导入导出：
  - 新增 `GET /config/export`
  - 新增 `POST /config/import`
  - 首页顶部增加导出配置入口
  - 数据源抽屉增加配置导入/导出区

验证：

- 使用 Schema JSON / SQL 验证 `SELECT *` 可展开为具体字段
- Docker 容器内完整单元测试通过：

```bash
docker compose exec app python -m unittest discover -s tests -v
```

## 23. 流式对比与动态 SQL 血缘增强

新增：

- 流式分块对比：
  - 任务配置新增 `stream_compare`
  - 开启后通过数据库游标迭代器按批读取源/目标结果集
  - 对比阶段使用主键有序归并，减少源/目标完整结果集常驻内存
  - 要求源 SQL 和目标 SQL 都按主键升序排序；乱序或重复键会报错
- 动态 SQL 血缘增强：
  - 支持从 `EXECUTE IMMEDIATE 'insert ... select ...'` 中提取可分析 SQL
  - 支持从 `exec sp_executesql N'insert ... select ...'` 中提取可分析 SQL
  - 动态 SQL 中的 `SELECT *` 可结合 Schema 元数据展开字段级落表映射

验证：

- 新增流式归并结果一致性和有序性校验测试
- 新增 Oracle 动态 SQL 和 SQL Server 动态 SQL 血缘测试
- Docker 容器内完整单元测试通过：

```bash
docker compose exec app python -m unittest discover -s tests -v
```
