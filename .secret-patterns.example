# .secret-patterns.local 的模板。
#
# 用法：
#   cp .secret-patterns.example .secret-patterns.local
# 然后在 .secret-patterns.local 里写本团队私有的、不能进仓库的敏感信息正则。
# .secret-patterns.local 已 gitignore，永不入库。pre-commit hook 会自动读取
# 它并附加到通用规则后面。
#
# 格式：一行一个 ERE（扩展正则），# 开头是注释行，空行忽略。
#
# 重要：这个 .example 文件本身【只放占位示例】—— 不要在这里写任何真实 IP /
# key 文件名 / 主机名 / 运维用户名。真实值只写进 .secret-patterns.local。
#
# 下面的示例用 RFC 5737 文档专用网段（203.0.113.0/24）和 PLACEHOLDER 占位，
# 复制后把它们换成你环境里的真实值：
#
#   # 生产实例公网 IP
#   203\.0\.113\.[0-9]+
#
#   # 部署用 SSH key 文件名
#   PLACEHOLDER_deploy_key\.pem
#
#   # 运维登录用户名前缀
#   PLACEHOLDER_opsuser@
#
#   # 内部数据库主机名
#   db-PLACEHOLDER\.internal
