# DataOps Studio 项目总结

## 项目定位

`DataOps Studio` 是一个面向离线 Windows 环境的数据对比、SQL 血缘、ETL 流程分析与元数据辅助工具。

技术特点：

- 后端：FastAPI
- 页面：Vue 3 SPA + Tailwind CSS；保留 Jinja2 经典页面作为兼容入口
- 配置：本地 JSON 文件
- 部署：支持本地 Python 运行，不依赖配置数据库
- 运行场景：离线 Windows 环境优先，Docker 仅用于开发验证

---

## 当前已有功能

### 1. 数据源管理

- 支持 `DM / MySQL / Oracle / DB2`
- 数据源配置保存到 `config/datasources.json`
- 支持新增、编辑、删除数据源
- 支持数据源连通性测试
- 支持驱动检测接口：`GET /api/drivers`

### 2. 对比任务管理

- 任务配置保存到 `config/tasks.json`
- 数据对比任务工作台采用左侧任务列表、右侧任务详情布局
- 支持任务搜索和按 SQL 模式筛选
- 支持新增、编辑、删除任务
- 支持复制任务
- 复制后的任务可直接重命名、修改 SQL、字段映射和对比规则
- 支持单 SQL 模式
- 支持双 SQL 模式
- 支持人工指定主键字段 `key_columns`

### 3. 数据对比能力

- 输出：
  - `only_source`
  - `only_target`
  - `diff`
  - `same`
- 支持字段映射
- 字段映射为空时，支持按源/目标查询结果字段顺序自动映射
- 支持忽略字段
- 主键、字段映射、忽略字段支持字段名大小写不敏感匹配
- 支持数值误差容忍
- 主键匹配自动去除字符串前后空格
- 支持字符串去前后空格
- 支持字符串忽略大小写
- 支持空字符串按空值处理

### 4. 执行能力

- 支持前台同步执行
- 支持后台异步执行
- 支持后台任务取消
- 支持任务执行状态查询
- 支持执行前预览源/目标前 N 行
- 任务编辑区展开时，预览支持使用当前未保存的草稿 SQL 和数据源
- 任务预览结果区域已放大，支持在页面中查看更长 JSON 预览并手动调整高度
- 支持最大查询行数限制
- 支持最大 Excel 导出行数限制
- 支持查询分块读取参数 `fetch_chunk_size`，大结果集查询时按批次读取并反馈后台进度
- 支持流式分块对比参数 `stream_compare`，按主键有序结果集边读取边归并，减少源/目标全量结果集常驻内存

### 4.1 页面交互

- DataOps Studio SPA 提供深色侧栏、顶部工具条和分块卡片式工作区
- 数据源管理为独立主页面，放在导航第一项
- 多脚本分析结果按“流程总览、数据流图、脚本清单、表级流转、跨脚本依赖、风险提示”分块切换展示，避免结果流水式堆叠
- 高级对比规则默认折叠，降低页面臃肿感
- 当前任务最近一次执行摘要在任务详情头部展示

### 5. 结果管理

- 结果写入 `results/`
- 支持 JSON 导出
- 支持 Excel 导出
- Excel 保留明细 sheet：
  - `only_source`
  - `only_target`
  - `diff`
  - `same`
- Excel 新增 `汇总对照` sheet
- 支持执行历史列表
- 支持按任务筛选执行历史
- 支持历史结果重新下载
- 支持删除历史结果
- 支持勾选多个历史结果并选择 sheet 合并导出到一个 Excel

### 6. SQL 安全保护

- 只允许 `SELECT` / `WITH`
- 禁止多语句
- 禁止 `SELECT ... FOR UPDATE`
- 禁止：
  - `INSERT`
  - `UPDATE`
  - `DELETE`
  - `MERGE`
  - `DROP`
  - `ALTER`
  - `TRUNCATE`

### 7. SQL 血缘分析

- 基于 `sqlglot`
- 不连接数据库
- 不执行 SQL
- 支持页面输入 SQL
- 支持导入 `.sql` / `.txt`
- 支持：
  - `SELECT`
  - `WITH`
  - `JOIN`
  - `WHERE`
  - `GROUP BY`
  - 聚合字段
  - `UNION / UNION ALL`
- 支持 `INSERT INTO ... SELECT ...` 落表字段映射
- 支持脚本变量和占位符识别，例如 `${v_qmrq_m}`、`:biz_date`、`@biz_date`
- 支持从 ETL 脚本中跳过 `DELETE`、`COMMIT` 等非血缘主体语句，提取多段 `INSERT INTO ... SELECT ...`
- 支持从 `CREATE PROCEDURE` / `CREATE FUNCTION` 过程体中提取可分析的 `INSERT SELECT`
- 支持从 `EXECUTE IMMEDIATE`、`sp_executesql` 等动态 SQL 字符串中提取可分析的 `INSERT SELECT`
- 支持 CTE、复杂派生表、嵌套子查询和 `UNION` 子查询字段展开到真实来源表
- 支持在字段血缘和落表字段映射中展示变量引用
- 支持上传 Schema 元数据 JSON / SQL / TXT / ZIP，用于展开 `SELECT *`，并增强动态 SQL / 过程体中的字段级血缘
- 单脚本和多脚本血缘分析均支持一次上传多个 Schema 元数据文件，多个文件会按表名合并字段
- 支持表级血缘图展示
- 支持血缘分析结果导出 JSON / Excel
- Excel 导出包含脚本变量 sheet
- 血缘分析页面操作区已优化为集中按钮栏，保留分析、格式化、导出 Excel、导出 JSON
- 支持多脚本 ETL 流程分析页面 `/lineage/batch`
- 支持多选上传 `.sql` / `.txt`，以及上传包含脚本文件的 `.zip`
- 多脚本分析的 Schema 元数据也支持多选 `.json` / `.sql` / `.txt`，或上传包含元数据文件的 `.zip`
- 多脚本分析输出脚本清单、表级数据流、跨脚本依赖和风险提示
- 多脚本分析可识别外部源表、最终产物、多脚本写同一目标表、`SELECT *`、疑似动态 SQL 和解析失败文件
- 多脚本分析支持表级流程图展示

### 8. SQL 辅助能力

- 血缘分析页面保留 SQL 格式化能力
- 任务编辑页支持对源 SQL / 目标 SQL 分别进行：
  - SQL 格式化
  - 输出字段提取
  - 候选 key 字段提示
  - SQL 只读校验
- 候选 key 是静态启发式提示，不自动替代人工主键配置
- 候选 key 识别规则包括：
  - `id`
  - `uuid`
  - `*_id`
  - `*_no`
  - `*_num`
  - `*_code`
  - `*_cd`
  - 包含 `num` 的字段
- `SELECT *` 场景会提示无法静态展开真实字段，需要显式列出字段名

### 9. 日志

- 日志写入 `logs/app.log`
- 记录任务执行、查询耗时、异常堆栈、血缘分析开始/完成
- 5MB 轮转，保留 5 个历史文件

### 10. 数据库兼容处理

- MySQL / OceanBase MySQL：连接和读写超时
- MySQL 8 默认认证场景建议同时准备 `pymysql` 和 `cryptography`
- DM：连接方式兼容处理
- DB2：`os.add_dll_directory` + `clidriver` 路径兼容处理

### 11. 配置导入导出

- 支持导出当前数据源和任务配置为 JSON
- 支持从 JSON 覆盖导入数据源和任务配置
- 导入前会校验数据源和任务结构，避免写入明显无效配置

---

## 版本演进记录

### 版本 1：基础版

完成内容：

- 数据源管理
- 对比任务管理
- 单 SQL / 双 SQL 对比
- JSON / Excel 导出
- 驱动检测
- SQL 安全校验
- Docker 本地测试支持

### 版本 2：SQL 血缘分析版

完成内容：

- SQL 血缘分析页面
- `.sql/.txt` 文件导入
- JOIN / WHERE / GROUP BY / UNION 识别
- 表别名识别修复
- 子查询 JOIN 展示优化

### 版本 3：ETL 血缘增强版

完成内容：

- `INSERT INTO ... SELECT ...` 落表字段映射
- 目标字段与 SELECT 别名不一致场景支持
- 子查询别名字段展开到真实来源表

### 版本 4：能力增强版

完成内容：

- 数据源测试连接
- 对比规则增强
- 执行历史
- 后台异步执行
- 大结果集保护
- SQL 辅助 API 和页面入口

### 版本 5：2026-04-24 热修复版

完成内容：

- 修复双 SQL 模式下主键与字段映射冲突问题
- 修复源/目标主键值前后空格导致数据误入 `only_source` / `only_target` 的问题
- 修复 DB2 数据源连通性测试 SQL 兼容问题
- 增强血缘分析中子查询别名来源表展开
- 新增血缘分析结果 JSON / Excel 导出
- 数据源连接测试反馈优化为：
  - 成功：`连接成功`
  - 失败：`连接失败：错误信息`
- 编辑区展开/收起交互优化
- 新增结果历史按任务筛选
- 新增任务复制能力，复制后自动展开编辑区，便于立即重命名和修改配置
- 修复数据库返回字段大小写与配置不一致时的主键缺失问题
- 修复编辑任务未保存时预览仍使用旧 SQL 的问题
- 新增字段映射为空时按查询字段顺序自动映射
- 新增历史结果多选合并导出，可选择 `汇总对照` / 明细 sheet 汇聚到一个 Excel
- 首页重构为任务工作台，数据源管理改为抽屉式入口
- 修复任务详情保存按钮遮挡预览输出的问题
- 补充 MySQL Docker 容器连接验证说明和 MySQL 8 驱动依赖提示
- 移除首页底部独立最近执行模块，改为当前任务最近一次执行摘要
- 字段映射输入体验优化，支持多种分隔符：
  - `->`
  - `=>`
  - `→`
  - `:`
  - `：`

### 版本 6：2026-04-25 SQL 辅助打磨版

完成内容：

- 血缘分析页面精简为只保留当前 SQL 文本框格式化能力
- 任务编辑页源 SQL / 目标 SQL 增加轻量 SQL 辅助按钮：
  - `格式化`
  - `提取字段`
- 修复候选 key 没有识别 `CLIENT_ID`、`FUND_ACC_NO`、`SEC_CODE` 等字段的问题
- 候选 key 规则扩展到 `*_code`、`*_cd` 等常见代码字段
- 字段提取改为直接从 `SELECT` 表达式读取输出列，降低依赖血缘结果导致的展示偏差
- 前端增加候选 key 兜底判断，避免后端返回字段但未返回候选 key 时页面无提示
- 对 `SELECT *` 增加明确提示：静态解析无法展开真实字段，需显式列出字段名
- 新增 SQL 辅助回归测试，覆盖字段提取、候选 key 和格式化行为

### 版本 7：2026-04-25 血缘增强与界面优化版

完成内容：

- 血缘分析支持脚本变量和占位符识别：
  - `${v_qmrq_m}`
  - `:biz_date`
  - `@biz_date`
- 血缘分析支持 ETL 脚本级解析，可跳过 `DELETE`、`COMMIT` 等非血缘主体语句，并提取多段 `INSERT INTO ... SELECT ...`
- 血缘分析支持从 `CREATE PROCEDURE` / `CREATE FUNCTION` 过程体中提取可分析的 `INSERT SELECT`
- 增强 CTE、复杂派生表、嵌套子查询和 `UNION` 子查询字段到真实来源表的展开能力
- 来源表结果中过滤目标落表，避免 `dw.*` 目标表误入来源表
- 字段血缘和落表字段映射新增变量引用信息
- 血缘 Excel 导出新增 `脚本变量` sheet
- 血缘分析页面操作区改为集中按钮栏：
  - `分析血缘`
  - `格式化`
  - `导出 Excel`
  - `导出 JSON`
- 任务预览输出区域放大，并支持手动调整高度
- 新增血缘分析回归测试，覆盖脚本变量、多段 INSERT、复杂派生表和 CTE 字段回溯

### 版本 8：2026-04-25 多脚本 ETL 流程分析原型

完成内容：

- 新增 `/lineage/batch` 多脚本 ETL 流程分析页面
- 支持批量上传 `.sql` / `.txt`
- 支持上传包含 `.sql` / `.txt` 的 `.zip` 脚本包
- 新增脚本清单汇总：
  - 文件名
  - 解析状态
  - 语句数
  - 读取表
  - 写入表
  - 变量
  - 文件级提示
- 新增表级数据流汇总：
  - 来源表
  - 目标表
  - 所属脚本
  - 语句序号
- 新增跨脚本依赖识别：脚本 A 写出的表被脚本 B 读取时，形成脚本依赖边
- 新增风险提示：
  - 解析失败
  - `SELECT *`
  - 疑似动态 SQL
  - 多脚本写同一目标表
  - 外部源表
  - 最终产物
- 首页和单脚本血缘页面增加“多脚本分析”入口
- 新增多脚本分析回归测试，覆盖表级流转、跨脚本依赖、变量和风险提示

### 版本 9：2026-04-25 运维与血缘增强版

完成内容：

- 后台异步任务新增取消能力：
  - 新增 `POST /api/runs/{job_id}/cancel`
  - 首页后台执行后展示“取消后台任务”按钮
  - 取消为协作式取消，正在执行的数据库查询会在当前阶段结束后停止后续流程
- 任务运行限制新增 `fetch_chunk_size`
- 数据库查询改为支持按批次读取，并向后台任务状态反馈已读取行数
- 单脚本血缘页面新增表级血缘图
- 多脚本 ETL 页面新增表级流程图
- 单脚本血缘页面支持上传一个或多个 Schema 元数据 JSON / SQL / TXT，也支持 Schema ZIP 包，用于展开 `SELECT *`
- 新增配置导入导出：
  - `GET /config/export`
  - `POST /config/import`
  - SPA 数据源管理主页面和经典首页提供入口
- 新增回归测试，覆盖 Schema 展开、分块读取、任务取消和配置导入导出

### 版本 10：2026-04-25 流式对比与动态 SQL 血缘增强版

完成内容：

- 任务运行限制新增 `stream_compare`
- 开启流式分块对比后，源/目标查询通过迭代器按批读取，不再先把两边完整结果集全部物化到内存
- 流式对比使用主键有序归并算法，要求源 SQL 和目标 SQL 的结果都按主键升序排序；发现重复键或乱序时会直接报错
- 首页任务高级配置新增“流式分块对比（SQL 按主键排序）”
- 血缘分析新增动态 SQL 字符串提取：
  - `EXECUTE IMMEDIATE 'insert ... select ...'`
  - `exec sp_executesql N'insert ... select ...'`
- 动态 SQL 中的 `SELECT *` 可结合 Schema 元数据展开为字段级落表映射
- 新增回归测试，覆盖流式归并、有序性校验、Oracle 动态 SQL 和 SQL Server 动态 SQL

## 离线部署说明

适用环境：

- Windows
- Python 3.12 已安装
- 禁止使用 Docker
- 离线环境可通过本地 `wheels` 目录安装全部 Python 依赖

离线部署步骤：

1. 解压部署包
2. 确认 `wheels` 目录包含 `requirements.txt` 中全部依赖
3. 运行 `install_dependencies_offline.bat`
4. 运行 `start.bat`
5. 浏览器打开 `http://127.0.0.1:8000/spa`

---

## 项目目录说明

- `config/datasources.json`：数据源配置
- `config/tasks.json`：任务配置
- `results/`：结果文件
- `logs/app.log`：运行日志
- `templates/`：页面模板
- `static/`：静态资源，`static/spa/` 为已构建的 SPA 前端
- `app/`：后端代码
- `tests/`：单元测试

---

## 当前建议的后续方向

可继续增强的方向包括：

- 流式对比结果落盘与分页读取，进一步降低 `diff` / `same` 明细聚合对内存的占用
- 动态 SQL 拼接表达式求值、运行时分支建模和跨过程调用关系识别
- 基于 Schema 元数据继续增强跨脚本字段级血缘追踪
