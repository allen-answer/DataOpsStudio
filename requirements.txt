fastapi>=0.136.3
uvicorn[standard]>=0.48.0
jinja2>=3.1.6
python-multipart>=0.0.29
pydantic>=2.13.4
pandas>=3.0.3
openpyxl>=3.1.5
pyarrow>=24.0.0  # ParquetReader 后端；pandas DataFrame 的 parquet IO 也走它
sqlglot>=30.8.0
pyyaml>=6.0.3  # config/lineage_group_rules.yml 解析；JSON 格式仍可用，YAML 仅在文件后缀是 .yml/.yaml 时需要
bcrypt>=5.0.0  # 用户口令 hash（直接用 bcrypt 库，passlib 1.7.4 + bcrypt 5.x 兼容性差）
python-jose[cryptography]>=3.5.0  # JWT 编解码
pyotp>=2.9  # TOTP / MFA 第二因子（admin 二步验证）
faker>=40.19.1  # Phase 14: ai_filler v3 locale-aware fallback(provider=off 时仍能生成业务样本)
pytest>=9.0.3
httpx>=0.28.1   # TestClient 依赖：starlette.testclient 通过 httpx 驱动
APScheduler>=3.11.2
tzdata>=2026.2; sys_platform == "win32"  # Windows 没系统 zoneinfo（Linux 有 /usr/share/zoneinfo）,APScheduler 启动时 Asia/Shanghai 时区会找不到

# Optional database drivers. Install only the drivers needed by your environment.
dmPython
pymysql
cryptography  # Required by PyMySQL for MySQL 8 caching_sha2_password authentication.
# oracledb
# ibm_db
